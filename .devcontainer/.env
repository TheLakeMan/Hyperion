IDE_PORT=8443
IDE_PASSWORD=V6172117877
HYPERION_HOST_PATH=/home/thelakeman/Hyperion
# Image tag used for Docker Swarm deployments (local build)
IDE_IMAGE=hyperion-ide:swarm
# Space-separated list of VS Code extensions to preinstall inside code-server.
IDE_EXTENSIONS="ms-vscode.cpptools ms-vscode.cmake-tools llvm-vs-code-extensions.vscode-clangd twxs.cmake webfreak.debug ms-python.python ms-python.vscode-pylance streetsidesoftware.code-spell-checker yzhang.markdown-all-in-one esbenp.prettier-vscode eamodio.gitlens redhat.vscode-yaml ms-azuretools.vscode-docker"

# Optional custom TLS certificate configuration (if provided, HTTPS will be enabled).
# Uncomment and set the following variables, then mount the certificate directory in compose.yaml.
# CERT_FILE=/certs/ide.crt
# KEY_FILE=/certs/ide.key
